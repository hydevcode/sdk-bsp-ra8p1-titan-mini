/*
 * Compatibility header for WebNet 2.0.3 on RT-Thread trees where the old
 * dfs_posix.h header was split into standard POSIX headers.
 */
#ifndef __WEBNET_DFS_POSIX_COMPAT_H__
#define __WEBNET_DFS_POSIX_COMPAT_H__

#include <fcntl.h>
#include <unistd.h>
#include <sys/stat.h>
#include <dirent.h>

#endif /* __WEBNET_DFS_POSIX_COMPAT_H__ */
